# FastAPI and ASGI
fastapi==0.119.1
uvicorn[standard]==0.27.0
python-multipart==0.0.20

# Database（PostgreSQL 异步驱动 asyncpg；greenlet 为 SQLAlchemy async 硬依赖）
sqlalchemy==2.0.40
asyncpg==0.31.0
greenlet==3.4.0

# Redis
redis==7.4.0

# Celery
celery==5.6.3

# Pydantic
pydantic==2.11.7
pydantic-settings==2.11.0

# Milvus
pymilvus==2.6.2

# MinIO
minio==7.2.20

# HTTP Client
httpx==0.28.1

# MongoDB (FastGPT 同步，motor.motor_asyncio)
motor==3.7.1

# Evaluation Frameworks
# ragas 0.4.3 传递依赖已覆盖 langchain-community/tiktoken/nest-asyncio/appdirs 等，无需单独声明
ragas==0.4.3
datasets==4.2.0
openai==1.109.1
langchain==0.2.17
langchain-core==0.2.43
langchain-openai==0.1.25

# Document Processing（PyMuPDF 即 import fitz，PDF 文本提取；缺失时运行时 400 提示）
PyMuPDF==1.26.5

# Data Processing
pandas==2.2.3
numpy==1.26.4

# RSS 爬虫（feedparser 解析订阅源；trafilatura 正文提取；bs4+lxml 兜底解析；markdownify 转 Markdown）
feedparser==6.0.11
langdetect==1.0.9
trafilatura==1.8.1
beautifulsoup4==4.12.3
lxml==5.1.0
markdownify==0.14.1

# Development
pytest==8.2.1
pytest-asyncio==0.23.3

# Agent Sandbox（可选：缺失时 VibeAgent 回退本地 subprocess 执行）
agent-sandbox==0.0.30

# LangGraph 代码生成（VibeAgent 生成的工作流代码在沙箱中运行所需）
langgraph>=0.2.0
